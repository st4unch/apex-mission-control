---
date: 2026-06-21
project: claude-control-plane
type: handoff
---

# Handoff — claude-control-plane — 2026-06-21

## Ne yapıldı
- Terminal donma/tuş-gecikmesi şikayeti dev stres-harness (5 koşum) ile teşhis edildi → suçlu Tauri/React DEĞİL, PTY çıktı yolu.
- 2 düzeltme: binary Channel (ArrayBuffer, JSON şişmesi yok) + bounded `sync_channel(256)`+8ms paced flush (backpressure).
- Stres testi gizli **OOM/runaway bug** yakaladı (uygulama 9.3GB RAM'e fırladı) → backpressure ile kapatıldı.
- Sonuç: sel altında echo p95 **405ms→3ms**, runaway yok, frontend 14/14 + backend 14 test geçti.
- Commit `436e334` (local), versiyon 0.0.3'e bump'landı.

## Değişen dosyalar
- `src-tauri/src/pty.rs` — binary Raw event + bounded sync_channel + paced flush (asıl fix)
- `src/components/Terminal.tsx` — ArrayBuffer kontratı
- `src/perf/harness.ts` — yeni, dev-only stres harness (`VITE_APEX_PERF=1`)
- `src/main.tsx`, `src/App.tsx` — harness Profiler + dev hook (gated)
- `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json` — 0.0.3
- `docs/prd-apex-mission-control.progress.md` — sonuçlar

## Kararlar (gelecek oturumu bağlayan)
- Stack Tauri'de KALIR — donma app-code'du, framework değil (kanıt: IPC 0.5ms, React commit ≤7ms).
- Perf harness korunur (dev-only, prod'da inert) — ileride perf işi için tekrar kullanılır.

## Sıradaki iş
- [ ] `app-signing` agent'ı v0.0.3 build+notarize bitirsin → `/tmp/ApexMissionControl-0.0.3-arm64.zip` + spctl/stapler çıktısını doğrula.
- [ ] Kullanıcı imzalı v0.0.3'ü kurup GERÇEK "claude çalışırken terminale yazma" akışını doğrulasın (§3 — sadece o yapabilir).
- [ ] Opsiyonel: kalan max outlier (~200-300ms, 1/40, idle'da da var → GC/compositor hiccup) izlenebilir; p95 zaten ≤5ms.
- [ ] Commit local; istenirse remote push project-manager onayıyla (§6).

## Dikkat / tuzak
- Stres dev'i yeniden başlatmadan ÖNCE eski `apex-mission-control`/`vite` proseslerini öldür (port 1420 çakışır + stale HMR eski binary'den rapor yazar → yanlış sonuç).
- Flood testi koşarken memory watchdog kullan (backpressure'sız 9.3GB'a fırlamıştı).
