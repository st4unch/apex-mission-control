---
date: 2026-06-21
project: claude-control-plane
type: handoff
---

# Handoff — claude-control-plane — 2026-06-21

## Ne yapıldı
- **v0.0.5 YAYINLANDI** (commit `57dbc70`, GitHub release v0.0.5, imzalı+notarize): File>New File (⌘N native menü), branch detayı (Queue kartı), terminal dark/light toggle, **sayfa geçişinde terminal oturumu düşmesi** bug fix. Security+code review → bulgular düzeltildi.
- **zsh-autosuggestions** kuruldu + `~/.zshrc`'e eklendi (Warp/fish-vari gri history önerisi; uygulamada yeni terminal sekmesinde aktif).
- **App-wide light/dark mode YAZILDI ama COMMIT EDİLMEDİ / BENCE DOĞRULANMADI:** tek toggle tüm uygulamayı + terminal + Monaco'yu çevirir; varsayılan sistemi takip eder. Altyapı + 8 bileşene `dark:` varyantı (3 paralel subagent). Subagent'lar "tsc temiz" dedi; **ben full build/görsel doğrulama yapmadan kullanıcı "dur" dedi.**

## Değişen dosyalar (UNCOMMITTED — dark mode)
- `src/index.css` — `@custom-variant dark` (class-tabanlı) + html bg fallback
- `src/App.tsx` — `themeMode` state (system/light/dark) + matchMedia + `.dark` toggle; terminal/Monaco'ya `effectiveTheme`
- `src/components/FileEditor.tsx` — `theme` prop → Monaco `vs-dark`/`light`
- `src/components/{QueuePage,BranchDAG,SessionsPage,NewAgentModal,SessionMonitor,FileTree}.tsx` — `dark:` renk varyantları

## Kararlar (gelecek oturumu bağlayan)
- Tema: Tailwind v4 class-tabanlı dark (`.dark` on `<html>`); varsayılan `prefers-color-scheme`, toggle explicit set eder, localStorage `apex.theme`.
- Terminal + Monaco app temasını takip eder (ayrı toggle yok artık).

## Sıradaki iş
- [ ] Dark mode'u DOĞRULA: `npm run build` + çalışan uygulamada toggle ile tüm yüzey (sidebar/Queue/Sessions/modal/terminal/editor) birlikte dönüyor mu, kaçan light leke var mı.
- [ ] **BranchDAG SVG inline-hex** dark'a yanıt vermiyor (`dark:` alamaz) — ele al: `:247-248` glow `stopColor`, `:257-259` grid `stroke="#f3f4f6"`, `:273` bezier `stroke="#cbd5e1"`.
- [ ] Doğrulanınca commit + versiyon bump 0.0.6 + (onayla) release.
- [ ] Kullanıcı v0.0.5 özelliklerini gerçek kullanımda hâlâ smoke-test etmeli (branch detay + ⌘N doğrulanmadı; renk+new file kullanıcı onayladı).

## Dikkat / tuzak
- Notarization profili `apex-notary` (.p8 `~/Downloads/AuthKey_M87Y6CK4GH.p8`); zip her zaman `ditto -c -k --keepParent`, asla plain `zip`. Asset: `ApexMissionControl-<ver>-arm64.zip`.
- `tasks/` ve release zip'i commit'e GİRMEZ (lessons.md notarization key-id içerir).
- Perf harness (`VITE_APEX_PERF=1`) sadece pencere ODAKLI iken koşar (App Nap timer'ları askıya alır) — CLI'dan doğrulanamaz.
